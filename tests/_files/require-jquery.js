/**
 * Just some comment
 */

 var thisDataWillBeMinified = 'but is here to make sure';

 function thatTheDataToBeMinified(isNotToo) {
     alert('big because it\'ll otherwise take');

     tooLongTo('minify.');

     // So that's the reason for this
 };

 weirdfile('You\'re looking at');

 thatTheDataToBeMinified('Sorry for that.');
